version: "3.7"
services:
    db:
        image: postgres
        restart: always
        environment:
            POSTGRES_PASSWORD: root
        volumes:
            - pgdata:/var/lib/postgresql/data
    webapp:
        build:
            context: .
            dockerfile: WebsiteDockerfile
        depends_on:
            - db
        ports:
            - 8080:8080
        volumes:
            - logs:/logs
volumes:
    pgdata:
    logs:
